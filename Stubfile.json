{
        "development": {
          "dependencies": {
            "example": "staging"
          },

          // Route example.com to the local rails app
          "example.com": "localhost:3000",

          // Capture outgoing email (if no remote server is used for delivery)
          "smtp://.*": "about://blank" 
        },
}
